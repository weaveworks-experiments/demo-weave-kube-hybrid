# DigitalOcean
export TF_VAR_do_token=X

# AWS
export TF_VAR_aws_access_key=X
export TF_VAR_aws_secret_key=X

# GCE
# TODO

# SSH key
export TF_VAR_k8s_ssh_key=$(cat k8s-test.pub)

# Weave secret
export WEAVE_SECRET=$(python -c 'import random; print "%0x" % (random.SystemRandom().getrandbits(16*8),)')

# Kubernetes token
export TF_VAR_k8s_token=$(python -c 'import random; print "%0x.%0x" % (random.SystemRandom().getrandbits(3*8), random.SystemRandom().getrandbits(8*8))')
